<div class="element-wrapper">
    <label for="{{$id}}">{{$title}}</label>
    <input type="{{$type ?? 'text'}}" value="{{$value ?? ''}}" id="{{$id}}" name="{{$id}}" placeholder="{{$placeholder ?? ''}}">
</div>